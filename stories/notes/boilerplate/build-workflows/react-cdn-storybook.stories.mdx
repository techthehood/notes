import { Meta, Description, DocsContainer } from '@storybook/addon-docs/blocks';
  import react_cdn_storybook from '../../../../boilerplate/build-workflows/react-cdn-storybook.md';
  

  <Meta title="boilerplate/build_workflows/react cdn storybook"
    parameters={{
      docs: {
        container: ({ children, context }) => (
          <DocsContainer context={context}>
              <div className="d3-storybook-wrapper" style={{padding: '0 3rem'}}>{children}</div>
          </DocsContainer>
        ),
      },
    }}
   />

  <Description>{react_cdn_storybook}</Description>
