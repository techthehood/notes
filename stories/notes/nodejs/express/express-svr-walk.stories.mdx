import { Meta, Description, DocsContainer } from '@storybook/addon-docs/blocks';
import express_svr_walk from '../../../../nodejs/express/express server walkthrough.md';

<Meta title="nodejs/express/server walkthrough"
  parameters={{
    docs: {
      container: ({ children, context }) => (
        <DocsContainer context={context}>
            <div className="d3-storybook-wrapper" style={{padding: '0 3rem'}}>{children}</div>
        </DocsContainer>
      ),
    },
  }}
 />

<Description>{express_svr_walk}</Description>
